# Email Configuration
# Replace with your Gmail address
EMAIL_USER=your-email@gmail.com
# Replace with your Gmail App Password (not your regular password)
# To create an App Password: Google Account > Security > 2-Step Verification > App Passwords
EMAIL_PASSWORD=your-app-password
# Replace with the email address that should receive emergency reports
EMERGENCY_CONTACT_EMAIL=emergency-contact@example.com

# Server Configuration
PORT=3000
NODE_ENV=development

# Security
# Generate a random string for session security
SESSION_SECRET=your-session-secret-key 