{
  "name": "abuse-detection-system",
  "version": "1.0.0",
  "description": "Abuse detection and support system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "vercel-build": "echo 'No build step required'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nodemailer": "^6.9.7",
    "multer": "^1.4.5-lts.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": "18.x"
  }
}
